/**
 * LEGACY SERVER STUB
 *
 * Расширенный сервер (JWT + Supabase + persistence) удалён.
 * Используйте минимальный сервер: node server/index.ts
 * Этот файл оставлен временно, чтобы старые команды запуска упали с понятной ошибкой.
 */

console.error('[legacy] server/socket-server.mjs больше не используется. Запустите: node server/index.ts');
process.exit(1);
