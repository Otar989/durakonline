<div align="center">
<h1>Durak Online (Новый модульный core)</h1>
<p><em>Подкидной Дурак 36 карт. Новый чистый движок + offline/online режим, Drag & Drop, бот. Legacy код (Supabase / расширенный сервер) удалён.</em></p>
</div>

## ✨ Текущие возможности (новый core)

Основное:
- Подкидной Дурак 36 карт (2 игрока MVP) + опционально переводной режим
- Чистый движок (`game-core`): правила, `legalMoves`, `applyMove`, базовый бот
- Offline режим (локальный state + бот) и Online режим (Socket.IO)
- Re-Sync (ручной + авто) через сравнение hash состояния
- Авто-добавление бота после таймаута ожидания оппонента
- Автоматический fallback в offline при потере соединения

Игровой UI / UX:
- Drag & Drop атаки/защиты с подсветкой легальных действий
- Свайпы (mobile): → ВЗЯТЬ, ← единственная атака или БИТО, ↑ единственная защита, ↓ ВЗЯТЬ
- Горячие клавиши: A / D / R / T / E + B/V дублируют БИТО/ВЗЯТЬ
- Адаптивный `GameLayout`: сайдбар / стол / лог / рука с перестройкой под мобильные брейкпоинты
- Мобильная панель крупных кнопок действий (TAKE / DEFEND / BITO)
- Подсказки жестов (одноразовое модальное окно для мобильных)

Анимации:
- Полёты карт (hand→table, defend, draw deck→hand, table→discard) через собственный FLIP-слой + батч за один rAF
- Контроль скорости (0.5x–2x) и Reduced Motion toggle (persist в localStorage)
- Конфетти при победе

Аудио / Настройки:
- Web Audio `SoundManager` (unlock + iOS watchdog, ambient loop, многоканальный overlap)
- Звуки: attack(card), defend, take, bito, win, ambient, illegal, translate
- Volume, mute toggle, автоматическая разблокировка по первому жесту
- Темы: тёмная / светлая / системная с ранней инициализацией без вспышки
- Сохранение настроек (theme, sound, volume, animations)

Доступность (A11y):
- ARIA live regions (статус, drag, ход)
- `aria-pressed` для переключателей, групповая семантика action buttons
- Доступные кнопки для защиты (выполняют единственный вариант по Enter/Space)
- Skip link, фокус стили, семантические регионы

Производительность:
- Dynamic import тяжёлых компонентов (`TableBoard`, `MoveLog`, `FlipLayer`)
- Idle prefetch аудио ресурсов, preload основных SVG
- Отложенная регистрация Service Worker
- Сбор Web Vitals (LCP/CLS/FID) через PerformanceObserver + отправка `/api/vitals` (sendBeacon)

Прочее:
- Аватары (инициалы + hash-цвет)
- Индикаторы сети (reconnect тосты)
- Ограничение подкидываний количеству карт у защитника (min(6, карт у защитника))
- Тесты ядра (правила + translate edge)

Планируемые расширения см. в Roadmap.

## Удалённый legacy
Удалены: legacy zustand store, legacy socket hook, Supabase клиент и расширенный сервер. Оставлен минимальный сервер `server/index.ts` и минимальный движок (`game-core/*`, `src/lib/durak-engine.ts`). Все прежние заглушки удалены.

## 🚀 Запуск

Установить зависимости:
```bash
npm install
```

Запуск фронтенда и сокет-сервера вместе:
```bash
npm run dev:all
```

Отдельно (при необходимости):
```bash
npm run dev      # Next.js
npm run dev:ws   # socket.io сервер (порт 4001)
```

Откройте http://localhost:3000

## 🕹 Игровой процесс (онлайн)
1. Введите Room ID (или поделитесь ссылкой). Если её открыть — поле Room уже заполнено.
2. В лобби настройте «Переводной», при необходимости добавьте ботов.
3. Нажмите Старт.
4. Атакующий выкладывает карту (щелчок по доступной карте руки). Допустимы только ранги уже на столе (кроме первой).
5. Защитник выбирает атаку (клик по незакрытой) и затем карту для защиты.
6. Можно «Перевести» (TRANSLATE) — если режим включён, на столе только атаки одного ранга и ещё нет защит: положите карту того же ранга, роли меняются.
7. «Взять» — если защититься нельзя/не хотите.
8. «Бито» — когда все атаки закрыты.
9. На мобиле можно свайпом: вправо «Взять», влево «Бито» (если доступно).

### Быстрая ссылка с преднастройками
На странице `/create-game` формируется URL вида:
```
/?room=abc12&cfg=BASE64&auto=1
```
`cfg` — base64 от `encodeURIComponent(JSON.stringify(settings))`. После входа настройки применяются автоматически один раз.

## 🧠 Боты
Текущая логика:
1. Если можно выгодно перевести (у бота ≥2 карт, у оппонента карт ≥ чем у бота) — делает перевод самой низкой картой нужного ранга.
2. Иначе защищается минимальной приемлемой картой (предпочитая не-козыри).
3. Если защититься нельзя — берет.
4. В атаке бросает минимальную не-козырную, затем козыри.

## 🔒 Безопасность и правила
Сервер валидирует: допустимость атак (ранг, лимиты), защит, переводов и лимит карт стола (6).
Планируемые улучшения: более строгая валидация цепочек переводов с несколькими участниками, антиспам задержки.

## 🗺 Стек
Next.js (App Router) 15, React 19, TypeScript, Tailwind CSS 4, Socket.IO, Framer Motion (лениво), Web Audio (кастом), Sentry (ошибки), CSS design tokens.

## 📂 Структура (новая)
- `game-core/` – чистые типы, `engine.ts`, `bot.ts`, тесты
- `server/index.ts` – упрощённый Socket.IO сервер (2 игрока + бот)
- `ui/` – модульный UI (компоненты, хуки offline/online)
- `src/app/page.tsx` – точка входа (рендерит новый интерфейс)
- `src/app/api/vitals/` – приём Web Vitals
- `ui/context/SettingsContext.tsx` – глобальные настройки и аудио
- `ui/components/GameLayout.tsx` – адаптивный игровой layout
- `src/styles/tokens.css` – дизайн-токены / семантические переменные
<!-- legacy удалён -->

## Auth / Хранилище
На текущем этапе отсутствует. План: абстрактный storage слой + последующая интеграция (Supabase или иной сервис) без утяжеления клиента.

## 🌐 Переменные окружения
`.env.example` (минимум):
```
NEXT_PUBLIC_SOCKET_URL=
SOCKET_CORS_ORIGINS=
```

## 🛡 CORS (Socket)
`SOCKET_CORS_ORIGINS` парсится в списке. В dev можно оставить `*`. В прод — перечислите точные домены.

## 🏭 Прод деплой сокет-сервера
1. Node 20
2. Start Command: `node server/index.ts` (ts-node / предварительно компилировать если нужно)
3. PORT: `process.env.PORT || 4001`.
4. CORS ограничьте доменами в `SOCKET_CORS_ORIGINS` (через запятую).

## 🔐 Мини чеклист безопасности
- [x] Приватные руки рассылаются индивидуально
- [x] RLS включён на `raw_games`
- [ ] Ограничить спам действий (добавить rate-limit)
- [ ] Логи аномалий (подозрительные действия)

## 🔄 Roadmap (новый core)
1. Lighthouse: доп. анализ bundle / устранить лишние inline скрипты в пользу `<Script>`.
2. Улучшенный бот (оценка ценности козырей, вероятностные сценарии перевода).
3. История партий (persist + просмотр / реплей).
4. Lazy hydration части UI (hand / log) под интерактив.
5. Edge тесты доп. сценариев chain переводов.
6. Rate-limit и аномалия мониторинг (Sentry breadcrumbs событий).
7. PWA улучшения (офлайн кэш статики, fallback экран).
8. Доп. accessibility аудит (контраст, ручной обход screen reader).

## 🐛 Разработка
PR и issue приветствуются. Запускайте в режиме разработки и проверяйте логи сервера (порт 4001).

## 📜 Лицензия
MIT

# Durak Online – Prod Roadmap (MVP Premium)

## Быстрый старт

- Node 20.x
- ENV:
  - NEXT_PUBLIC_SOCKET_URL=http://localhost:4001
  - NEXT_PUBLIC_SUPABASE_URL=... 
  - NEXT_PUBLIC_SUPABASE_ANON_KEY=...
- Установка: `npm ci`
- Тесты: `npm test`
- Запуск фронта: `npm run dev` (Next.js)
- Запуск сервера сокетов: `npm run start` (порт 4001)

## Стек
- Next 15, TypeScript, Tailwind, Framer Motion, Howler, PWA
- Node + Socket.IO (валидация ходов на сервере)
- Supabase (Auth/Postgres)

## Миграции Supabase
SQL в `supabase/migrations/`:
- `0002_schema_core.sql` – users, profiles, wallets, matches, ratings, inventories, seasons, leaderboards

## Заметки
- Ядро правил в `game-core/` c тестами (Vitest)
- UI подсвечивает только легальные ходы, сервер валидирует тем же ядром
- OFFLINE режим – против бота

